name: Linux Desktop with XFCE and Cloudflare Tunnel

on:
  workflow_dispatch:

jobs:
  remote-desktop:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install XFCE4, VNC, and prerequisites
        run: |
          set -e
          sudo apt update
          sudo apt install -y xfce4 xfce4-goodies tightvncserver
          sudo apt install -y wget curl unzip dbus-x11

      - name: Install Firefox without Snap
        run: |
          set -e
          sudo add-apt-repository ppa:mozillateam/ppa -y
          sudo apt update
          sudo apt install -y firefox-esr

      - name: Setup and start VNC server
        run: |
          set -e
          mkdir -p ~/.vnc
          echo "123456" | vncpasswd -f > ~/.vnc/passwd
          chmod 600 ~/.vnc/passwd
          tightvncserver :1 -geometry 1280x800 -depth 24
          echo "VNC server started."

      - name: Create Cloudflare Tunnel
        run: |
          set -e
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared-linux-amd64
          ./cloudflared-linux-amd64 tunnel --url http://localhost:5901 &> cloudflare.log &
          sleep 10
          echo "Cloudflare Tunnel URL:"
          grep -o 'https://[^ ]*\.trycloudflare\.com' cloudflare.log || echo "URL not found. Check cloudflare.log."

      - name: Keep session alive
        run: |
          echo "XFCE desktop is ready. This workflow will run up to 6 hours."
          while true; do sleep 300; done
